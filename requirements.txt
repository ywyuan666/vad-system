# ── VAD 系统依赖 ──────────────────────────────────────────────────────
# 核心
numpy>=1.24.0
scipy>=1.10.0

# 音频处理
soundfile>=0.12.0
librosa>=0.10.0

# 深度学习（DNN VAD 需要，可选）
torch>=2.0.0
torchaudio>=2.0.0

# 配置
PyYAML>=6.0

# Web Demo（可选）
gradio>=4.0.0

# 数据可视化（可选）
matplotlib>=3.7.0

# 数据处理（Common Voice 需要，可选）
pandas>=2.0.0

# 行业基线对比（可选）
webrtcvad>=2.0.10

# ── 生产推理服务（可选）────────────────────────────────────────────
# FastAPI 推理服务器
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
prometheus-client>=0.19.0
python-multipart>=0.0.6

# 实时麦克风 Demo（可选）
sounddevice>=0.4.6

# 代码质量（可选）
pre-commit>=3.6.0
ruff>=0.3.0
mypy>=1.8.0
